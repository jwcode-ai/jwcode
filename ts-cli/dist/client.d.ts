import type { WSMessage } from './protocol.js';
type Handler = (msg: WSMessage) => void;
export declare class JwCodeClient {
    private ws;
    private handlers;
    private _running;
    private pingTimer;
    private reconnectDelay;
    private reconnectTimer;
    sessionId: string | null;
    backendUrl: string;
    wsUrl: string;
    token: string;
    constructor(backendUrl: string, wsUrl: string, token?: string);
    on(msgType: string, handler: Handler): () => void;
    connect(): Promise<string>;
    private _createSession;
    private _startReadLoop;
    private _startHeartbeat;
    private _stopHeartbeat;
    private _scheduleReconnect;
    private dispatch;
    send(msgType: string, message?: string, data?: Record<string, unknown>): void;
    chat(content: string, planMode?: boolean): void;
    stop(): void;
    pause(): void;
    resume(): void;
    planConfirm(): void;
    updateDocs(): void;
    doctor(): void;
    rewind(): void;
    compact(): void;
    switchModel(model: string): void;
    approveHook(approvalId: string): void;
    denyHook(approvalId: string): void;
    close(): Promise<void>;
}
export {};
